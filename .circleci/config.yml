version: 2
jobs:
  build:
    docker:
      - image: circleci/python:3.6
    steps:
      - checkout
      - run: sudo pip install pipenv virtualenv
      - run: pipenv install
      - run: pipenv run pytest --version
